# Start behavior parity with core tests

Behavior parity for the first Swift version will be established first through `BSMCoreTests`. The initial tests will cover code buffering, marker mapping, selection mode, paging, wildcard matching, candidate counts, database ordering, and possible next codes, while app-level InputMethodKit and candidate window verification can follow once the Swift app target builds.
