# Target macOS 14 for the modern Swift input method

The modern Swift input method will target macOS 14 or later. This establishes a contemporary SwiftUI/AppKit baseline for the rewrite instead of preserving the legacy project's macOS 10.9 deployment target.
